apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.fullname  }}
  labels:
    {{- .Values.labels | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.nodePort }}
      protocol: TCP
      name: http
  selector:
    {{- .Values.selectorLabels | nindent 4 }}